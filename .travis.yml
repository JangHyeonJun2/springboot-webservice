language: java
jdk:
  - openjdk8

branches: # 1번
  only:
    - master
# Travis CI 서버의 Home
cache: # 2번
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.gradle'

script: "./gradlew clean build"  # 3번

# CI 실행 완료시 메일로 알람
notifications:  # 4번
  email:
    recipients:
      - epfvkdlxj@naver.com